% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_prices.R
\name{as_event_prices}
\alias{as_event_prices}
\alias{as_event_prices.event_prices}
\alias{as_event_prices.data.frame}
\alias{print.event_prices}
\alias{summary.event_prices}
\alias{plot.event_prices}
\title{Standardize traded event state prices as an \code{event_prices} object}
\usage{
as_event_prices(x, ...)

\method{as_event_prices}{event_prices}(x, ...)

\method{as_event_prices}{data.frame}(
  x,
  time = NULL,
  price = NULL,
  bid = NULL,
  ask = NULL,
  scale = 1,
  discount = 1,
  book = NULL,
  method = c("discount", "overround"),
  clip = ec_default_params()$clip,
  market_id = NULL,
  event_date = NULL,
  ...
)

\method{print}{event_prices}(x, ...)

\method{summary}{event_prices}(object, ...)

\method{plot}{event_prices}(x, ...)
}
\arguments{
\item{x}{A \code{data.frame}/\code{tibble} (or an existing \code{event_prices} object,
returned unchanged).}

\item{...}{Reserved for future methods.}

\item{time}{Name of the time column (character). If \code{NULL}, the first of
\code{time}, \code{date}, \code{timestamp}, \code{datetime}, \code{t} (case-insensitive) is
used.}

\item{price}{Name of the price/probability column (character). If
\code{NULL}, the first of \code{q}, \code{price}, \code{p}, \code{q_t}, \code{value}
(case-insensitive) is used. Ignored when \code{bid} and \code{ask} are given.}

\item{bid, ask}{Optional names of bid and ask columns; if both are given,
the mid quote is used as price.}

\item{scale}{Numeric, divisor applied to the raw price first (use
\code{scale = 100} for percent quotes; default 1).}

\item{discount, book, method}{Passed to \code{\link[=q_from_price]{q_from_price()}}.}

\item{clip}{Numeric length-2, clipping bounds applied to \code{q} before the
log-odds transform (default \code{c(0.01, 0.99)}, see
\code{\link[=ec_default_params]{ec_default_params()}}).}

\item{market_id}{Optional character label of the market.}

\item{event_date}{Optional \code{Date}/\code{POSIXct} of the scheduled event
(resolution) date.}

\item{object}{An \code{event_prices} object (for \code{summary()}).}
}
\value{
An \code{event_prices} tibble; see Details.
}
\description{
\code{as_event_prices()} turns a \code{data.frame}/\code{tibble} holding a time series
of traded event state prices or the implied event probabilities
(prediction-market contracts, betting quotes) into a standardized
\code{event_prices} tibble that all estimation and plotting functions of the
package understand.
}
\details{
The constructor follows a strict \emph{flag, don't drop} convention: raw
values are kept in \code{q_raw}, data problems are recorded in flag columns,
and nothing is silently deleted. The only structural interventions are
sorting by time and removing duplicated timestamps (keeping the first
occurrence, with a warning), because increments are undefined otherwise.

Columns of the returned object:
\describe{
\item{time}{\code{Date} or \code{POSIXct} timestamp (sorted, unique).}
\item{q_raw}{the raw input price after rescaling by \code{scale}.}
\item{q}{the normalized probability, \code{q_from_price(q_raw, ...)}.}
\item{flag_na}{\code{TRUE} where \code{q} is missing.}
\item{flag_clip}{\code{TRUE} where \code{q} falls outside the clipping bounds and
will be clipped before the log-odds transform in \code{\link[=event_clock]{event_clock()}}.}
}

The clipping bounds, market id, and event date are stored as attributes
(\code{clip}, \code{market_id}, \code{event_date}) and are picked up by
\code{\link[=event_clock]{event_clock()}}, \code{\link[=event_clock_path]{event_clock_path()}}, and the plotting functions.

\strong{Timezones.} A \code{POSIXct} time column is kept in the timezone it
carries; character timestamps are parsed as UTC. Window bounds passed
as \code{Date} to \code{\link[=event_clock]{event_clock()}} and friends are interpreted in the
series' timezone (with \code{to} covering the full day), so mixing \code{Date}
bounds with a non-UTC intraday series is safe.
}
\section{Functions}{
\itemize{
\item \code{print(event_prices)}: Print method; shows market, range, and flags.

\item \code{summary(event_prices)}: Summary method; returns a one-row tibble
including the full-sample event-clock estimate.

\item \code{plot(event_prices)}: Plot method; dispatches to \code{\link[=plot_q]{plot_q()}}.

}}
\examples{
data(brexit2016)
ep <- as_event_prices(brexit2016,
  time = "date", price = "q_leave",
  market_id = "Brexit: Leave", event_date = as.Date("2016-06-23")
)
ep
summary(ep)
}
