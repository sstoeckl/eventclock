% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clock.R
\name{event_clock_path}
\alias{event_clock_path}
\alias{plot.event_clock_path}
\title{Cumulative event-clock path}
\usage{
event_clock_path(
  x,
  from = NULL,
  to = NULL,
  sample_every = ec_default_params()$sample_every,
  clip = NULL,
  normalize = TRUE
)

\method{plot}{event_clock_path}(x, ...)
}
\arguments{
\item{x}{An \code{event_prices} object (see \code{\link[=as_event_prices]{as_event_prices()}}), or a
\code{data.frame} coercible to one.}

\item{from}{Valuation date/time (default: first observation).}

\item{to}{One or more horizon dates/times (default: last observation =
full sample). Names are used as horizon labels.}

\item{sample_every}{Integer, use every k-th observation (sparse-sampling
robustness; default 1).}

\item{clip}{Numeric length-2 clipping bounds for \code{q} before the
log-odds transform; defaults to the bounds stored in \code{x}.}

\item{normalize}{Logical; if \code{TRUE} (default) also return \code{A_frac}, the
clock normalized to \eqn{[0, 1]} over the window.}

\item{...}{Unused (for the \code{plot} method).}
}
\value{
A tibble of class \code{event_clock_path} with columns \code{time}, \code{q},
\code{L} (clipped log-odds), \code{dL}, \code{dA} (squared increment), \code{A}
(cumulative clock), and — with \code{normalize = TRUE} — \code{A_frac} and
\code{cal_frac} (fraction of calendar time elapsed). Attributes carry the
market id and event date.
}
\description{
Computes the running (cumulative) event clock
\eqn{\widehat A_t = \sum_{s \le t} (\Delta L_s)^2} — the object behind
the "clock plot": how much of the eventual information had arrived by
each calendar date.
}
\section{Methods (by generic)}{
\itemize{
\item \code{plot(event_clock_path)}: Plot method; dispatches to \code{\link[=plot_clock]{plot_clock()}}.

}}
\examples{
data(brexit2016)
ep <- as_event_prices(brexit2016, time = "date", price = "q_leave")
path <- event_clock_path(ep)
tail(path)
}
