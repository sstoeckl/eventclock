% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{q_from_ffutures}
\alias{q_from_ffutures}
\title{Meeting-implied probability from a fed funds futures price}
\usage{
q_from_ffutures(price, pre_rate, meeting_date, step = 0.25)
}
\arguments{
\item{price}{Futures price(s), e.g. \code{95.21}.}

\item{pre_rate}{Effective rate prevailing before the meeting (percent,
e.g. \code{5.33}).}

\item{meeting_date}{Decision date(s) (\code{Date}); see \link{fomc_meetings}.}

\item{step}{Assumed move size in percentage points (default \code{0.25}).}
}
\value{
A tibble with columns \code{meeting_date}, \code{implied_avg},
\code{implied_post}, \code{delta_rate}, and \code{q} (probability of one \code{step}
move; sign of \code{delta_rate} gives the direction).
}
\description{
Converts the price of the 30-day fed funds futures contract for a
meeting month into the probability of a rate move of size \code{step},
using the standard month-average extraction: the contract settles on
the monthly average effective rate, the pre-meeting rate applies
through the decision day, so
\deqn{r_{post} = \frac{M\,\bar r - d\, r_{pre}}{M - d}, \qquad
  q = \frac{r_{post} - r_{pre}}{step},}
with \eqn{\bar r = 100 - \mathrm{price}}, \code{M} calendar days in the
month, and \code{d} the day of the month of the decision.
}
\details{
\code{q} is the probability of a single move of \code{step} under a two-point
assumption (no change vs. one move). Values outside \eqn{[0,1]}
indicate that more than one step (or a move of the opposite sign) is
priced: \code{q = 1.6} means 25bp fully priced plus a 60\% chance of a
second step; negative values indicate a priced move of the opposite
sign. Flag, don't drop. For a decision on the last day(s) of the
month the denominator degenerates — use the next-month contract.
Intermeeting moves and months with two meetings violate the two-point
assumption; flag such periods separately.
}
\examples{
# decision on the 15th of a 30-day month, pre-meeting rate 5.33\%,
# futures at 94.79 -> a 25bp cut is priced with ~96\% probability
q_from_ffutures(94.79, pre_rate = 5.33,
                meeting_date = as.Date("2024-09-15"), step = -0.25)
}
\seealso{
\link{fomc_meetings}, \code{\link[=q_from_price]{q_from_price()}}, \code{\link[=q_from_deal_spread]{q_from_deal_spread()}}
}
