% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polymarket.R
\name{pm_prices}
\alias{pm_prices}
\title{Download Polymarket price history for one outcome token}
\usage{
pm_prices(
  token_id,
  from,
  to,
  fidelity = 60,
  chunk_days = NULL,
  market_id = NULL,
  event_date = NULL
)
}
\arguments{
\item{token_id}{Character, the CLOB token id (from \code{\link[=pm_markets]{pm_markets()}}).}

\item{from, to}{Start and end of the window (\code{Date} or \code{POSIXct},
interpreted in UTC).}

\item{fidelity}{Integer, bar size in minutes (60 = hourly, 1440 =
daily; default 60).}

\item{chunk_days}{Integer, chunk length in days (default:
\code{max(1, floor(fidelity / 6))}, i.e. 10 days for hourly bars).}

\item{market_id}{Optional label stored on the result (default: the
token id).}

\item{event_date}{Optional scheduled event date stored on the result.}
}
\value{
An \code{event_prices} object with \code{time} (\code{POSIXct}, UTC) and \code{q}.
}
\description{
Pulls the price history of a CLOB outcome token from the public
\verb{/prices-history} endpoint and returns it as an \code{\link[=as_event_prices]{as_event_prices()}}
object. The Polymarket price is already a probability in \eqn{[0,1]}.
}
\details{
The endpoint caps the number of points per call (a few hundred), so the
requested window is split into chunks and stitched (deduplicated on the
timestamp). The default chunk length adapts to \code{fidelity}; with
\code{fidelity = 60} (hourly bars) chunks of 10 days are used.
}
\examples{
\dontrun{
mkts <- pm_markets("presidential-election-winner-2024")
tok <- mkts$token_id[grepl("Trump", mkts$question) & mkts$outcome == "Yes"]
ep <- pm_prices(tok, from = "2024-06-01", to = "2024-11-06")
event_clock(pm_daily(ep))
}
}
