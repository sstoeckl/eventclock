% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polymarket.R
\name{pm_daily}
\alias{pm_daily}
\title{Collapse intraday event prices to one daily snapshot}
\usage{
pm_daily(x, tz = "America/New_York", snapshot_hour = 16)
}
\arguments{
\item{x}{An \code{event_prices} object with intraday timestamps.}

\item{tz}{Character time zone of the snapshot (default
\code{"America/New_York"}).}

\item{snapshot_hour}{Numeric, snapshot cutoff hour in \code{tz} (default 16).}
}
\value{
An \code{event_prices} object with one (Date-typed) observation per
day.
}
\description{
Takes the last observation at or before \code{snapshot_hour} (local time in
\code{tz}) of each calendar day — the convention used to align prediction
market prices with market closes (e.g. 16:00 New York time for US
equities).
}
\details{
Seconds are deliberately truncated when comparing against the cutoff:
an observation stamped \code{16:00:59} still counts as \code{16:00}. API bars are
typically stamped a few seconds after the full hour they represent, so
minute precision is the robust convention for bar data. The stored
\code{event_date} attribute is coerced to \code{Date} to match the collapsed
time scale.
}
\examples{
\dontrun{
ep <- pm_prices(tok, from = "2024-06-01", to = "2024-11-06")
daily <- pm_daily(ep)
}
}
