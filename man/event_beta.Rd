% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta.R
\name{event_beta}
\alias{event_beta}
\alias{print.event_beta}
\title{Event beta: realized loading of asset returns on event-probability news}
\usage{
event_beta(asset, x, deta = NULL, controls = NULL, lags = NULL)

\method{print}{event_beta}(x, ...)
}
\arguments{
\item{asset}{A \code{data.frame} with a time column and either a return
column (\code{ret}) or a price column (\code{adjusted}, \code{close}, or \code{price};
log returns are computed).}

\item{x}{An \code{event_prices} object (or coercible); its probability
innovations \eqn{\Delta q_t} are the regressor. Observations are
matched on the calendar date.}

\item{deta}{Optional externally measured event exposure
\eqn{\Delta\eta}; enables the \eqn{\beta = 1} test.}

\item{controls}{Optional \code{data.frame} with a time column and control
variables (e.g. market returns), matched on date.}

\item{lags}{Newey-West lag order; default
\eqn{\lfloor 4 (n/100)^{2/9} \rfloor}. Use \code{lags = 0} for
heteroskedasticity-robust (HC0) errors.}

\item{...}{Unused (for the \code{print} method).}
}
\value{
An object of class \code{event_beta}: a list with
\item{coefficients}{tibble of terms, estimates, HAC standard
errors, t-statistics, and p-values.}
\item{deta_hat, deta_se}{the slope on \eqn{\Delta q} and its SE.}
\item{eta1, eta2}{model-implied levels (see Details).}
\item{beta, beta_se, beta_z, beta_p}{(only with \code{deta}) the loading
\eqn{b/\Delta\eta} and the Wald test of \eqn{\beta = 1}.}
\item{r2, n, lags}{regression diagnostics; \code{r2} is the realized
variance share of event news over the sample.}
}
\description{
Regresses asset returns on the innovations of a traded event
probability, \deqn{r_t = \alpha + b\, \Delta q_t + \gamma' c_t + u_t,}
with Newey-West (Bartlett/HAC) standard errors. To first order the
model implies \eqn{r_t \approx \Delta\eta\,\Delta q_t}, so the slope
\code{b} \emph{is} the returns-based estimate of the event exposure
\eqn{\widehat{\Delta\eta}}.
}
\details{
\strong{What is (and is not) identified.} Returns identify only the
\emph{spread} \eqn{\Delta\eta = \eta_1 - \eta_2}, not the
outcome-conditional levels \eqn{\eta_1, \eta_2} separately (those
require event-spanning option smiles). Consequently the loading test
\eqn{\beta = 1} is only meaningful against an \emph{externally} measured
exposure: supply \code{deta} (e.g. option-implied, or from an independent
sample) and the function reports \eqn{\beta = b / \Delta\eta} with a
Wald test of \eqn{\beta = 1}. Without \code{deta}, the regression is
exactly identified and only \eqn{\widehat{\Delta\eta}} is reported.

Under the risk-neutral adding-up constraint
\eqn{q\,\eta_1 + (1-q)\,\eta_2 = 1}, point estimates of the levels can
be backed out as \eqn{\eta_1 = 1 + (1-q)\Delta\eta} and
\eqn{\eta_2 = 1 - q\Delta\eta}; these are model-implied, not
independently identified, and are returned for convenience
(evaluated at the sample-average \code{q}).
}
\section{Methods (by generic)}{
\itemize{
\item \code{print(event_beta)}: Print method.

}}
\examples{
data(djt2024)
data(polymarket2024)
ep <- pm_daily(as_event_prices(polymarket2024))
event_beta(djt2024, ep)
}
\seealso{
\code{\link[=ec_relevance]{ec_relevance()}} for the pricing-relevance screen.
}
