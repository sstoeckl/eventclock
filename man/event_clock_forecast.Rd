% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clock.R
\name{event_clock_forecast}
\alias{event_clock_forecast}
\title{Real-time event-clock forecast}
\usage{
event_clock_forecast(
  x,
  at = NULL,
  horizon,
  trailing = ec_default_params()$trailing,
  sample_every = ec_default_params()$sample_every,
  clip = NULL
)
}
\arguments{
\item{x}{An \code{event_prices} object (see \code{\link[=as_event_prices]{as_event_prices()}}), or a
\code{data.frame} coercible to one.}

\item{at}{Valuation date/time at which the forecast is made (default:
last observation).}

\item{horizon}{Forecast horizon(s): either numeric (days) or
\code{Date}/\code{POSIXct} horizon dates (converted to days from \code{at}). Names
are used as labels.}

\item{trailing}{Integer, number of trailing observations in the
estimation window (default 40, the paper's headline choice; 20 and 60
are common robustness settings).}

\item{sample_every}{Integer, use every k-th observation (sparse-sampling
robustness; default 1).}

\item{clip}{Numeric length-2 clipping bounds for \code{q} before the
log-odds transform; defaults to the bounds stored in \code{x}.}
}
\value{
A tibble with columns \code{market_id}, \code{at}, \code{horizon},
\code{horizon_days}, \code{trailing}, \code{n_incr}, and \code{A_forecast}.
}
\description{
The real-time benchmark used in the accompanying working paper: estimate
the current information intensity from a trailing window and scale it to
the forecast horizon,
\deqn{\widehat A^{rt}(h) = \frac{RV(\text{trailing } k \text{ obs.})}
  {\text{calendar span in days}} \times h.}
}
\examples{
data(us2016)
ep <- as_event_prices(us2016, time = "date", price = "trump")
event_clock_forecast(ep,
  at = as.Date("2016-10-10"),
  horizon = c(`1W` = 7, `2W` = 14)
)
}
