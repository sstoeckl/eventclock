% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clock.R
\name{event_clock}
\alias{event_clock}
\title{Estimate event-clock (information) time from log-odds variation}
\usage{
event_clock(
  x,
  from = NULL,
  to = NULL,
  methods = ec_default_params()$methods,
  sample_every = ec_default_params()$sample_every,
  trunc_sd = ec_default_params()$trunc_sd,
  scale_fn = stats::mad,
  clip = NULL
)
}
\arguments{
\item{x}{An \code{event_prices} object (see \code{\link[=as_event_prices]{as_event_prices()}}), or a
\code{data.frame} coercible to one.}

\item{from}{Valuation date/time (default: first observation).}

\item{to}{One or more horizon dates/times (default: last observation =
full sample). Names are used as horizon labels.}

\item{methods}{Character vector of estimator variants; see Details.}

\item{sample_every}{Integer, use every k-th observation (sparse-sampling
robustness; default 1).}

\item{trunc_sd}{Numeric, truncation threshold in robust standard
deviations (default 3).}

\item{scale_fn}{Function computing the robust scale of the increments
for \code{truncated} (default \code{\link[stats:mad]{stats::mad()}}).}

\item{clip}{Numeric length-2 clipping bounds for \code{q} before the
log-odds transform; defaults to the bounds stored in \code{x}.}
}
\value{
A tibble with one row per horizon and method:
\item{market_id}{market label (from \code{x}).}
\item{from, to}{window bounds actually used.}
\item{horizon}{horizon label (names of \code{to}, or the date).}
\item{n_obs}{number of non-missing observations in the window.}
\item{n_incr}{number of increments used (after subsampling).}
\item{method}{estimator variant.}
\item{A}{the estimate \eqn{\widehat A_{t,T}}.}
}
\description{
The information accumulated about a scheduled event over a window
\eqn{[t, T]} — \emph{event-clock time} \eqn{A_{t,T}} — equals the quadratic
variation of the log-odds of the traded event probability,
\eqn{A_{t,T} = [\mathrm{logit}(q)]_{t,T}}. \code{event_clock()} estimates it
by the realized variation
\deqn{\widehat A_{t,T} = \sum_i \left[\mathrm{logit}(q_{t_{i+1}}) -
  \mathrm{logit}(q_{t_i})\right]^2}
together with standard robustness variants.
}
\details{
Because quadratic variation ignores finite-variation drift and is
invariant under equivalent measure changes, \eqn{\widehat A} is
\emph{measure-robust}: it does not require a martingale assumption under the
physical measure, and any (approximately) constant level distortion of
\code{q} — discounting, a constant state-price tilt, a constant cross-market
wedge — drops out entirely.

Available \code{methods}:
\describe{
\item{\code{rv}}{plain realized variation (the baseline).}
\item{\code{truncated}}{drops increments larger than \code{trunc_sd} robust
standard deviations, where the robust scale is \code{scale_fn(dL)}
(\code{\link[stats:mad]{stats::mad()}} by default). Note that with daily data and short
windows this criterion is coarse; reference results reported as
"truncated" often coincide with dropping the single largest
increment (\code{largest1}). Compare both.}
\item{\code{bipower}}{bipower variation
\eqn{BV = \frac{\pi}{2}\sum_{i\ge 2} |\Delta L_i||\Delta L_{i-1}|},
a jump-robust companion; the gap \eqn{RV - BV} is a descriptive
\emph{jumpiness index}.}
\item{\code{largest1}, \code{largest2}}{realized variation after removing the
one or two largest absolute increments.}
}

Windows are defined in calendar time from the valuation date \code{from} to
each horizon date in \code{to} (they are \emph{anchored} windows, not trailing
ones). All observations in the window are used, including weekends if
the series has them.
}
\examples{
data(brexit2016)
ep <- as_event_prices(brexit2016,
  time = "date", price = "q_leave",
  market_id = "Brexit: Leave", event_date = as.Date("2016-06-23")
)
# the working paper's 1M valuation date and 1W/2W/1M horizons
event_clock(ep,
  from = as.Date("2016-05-24"),
  to = c(`1W` = as.Date("2016-05-31"), `2W` = as.Date("2016-06-07"),
         `1M` = as.Date("2016-06-23"))
)
}
\seealso{
\code{\link[=event_clock_path]{event_clock_path()}} for the cumulative clock,
\code{\link[=event_clock_forecast]{event_clock_forecast()}} for the real-time benchmark.
}
