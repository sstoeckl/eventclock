% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{ec_validate}
\alias{ec_validate}
\alias{print.ec_validation}
\title{Data-quality report for an event-probability series}
\usage{
ec_validate(x)

\method{print}{ec_validation}(x, ...)
}
\arguments{
\item{x}{An \code{event_prices} object (or coercible).}

\item{...}{Unused (for the \code{print} method).}
}
\value{
A one-row tibble of class \code{ec_validation} with columns
\code{market_id}, \code{n_obs}, \code{start}, \code{end}, \code{spacing_days}, \code{n_na},
\code{share_clip}, \code{share_zero_incr}, \code{max_stale_run}, \code{tick},
\code{n_gaps}, \code{max_gap_days}, \code{max_abs_dL}, and \code{outlier_ratio}
(\code{max_abs_dL} / robust SD). Printed as a formatted report.
}
\description{
Screens an \code{event_prices} object for the data problems that matter for
clock estimation, following the flag-don't-drop philosophy: nothing is
altered, everything is reported.
}
\details{
The checks:
\itemize{
\item \strong{Coverage} — observations, span, median spacing, missing values,
share outside the clipping bounds.
\item \strong{Staleness} — share of zero increments and the longest run of
unchanged quotes; long stale runs depress \eqn{\widehat A}.
\item \strong{Tick size} — smallest non-zero price move; on a coarse tick grid
the logit of small-probability quotes moves in lumps
(microstructure noise; compare \code{\link[=ec_signature]{ec_signature()}}).
\item \strong{Gaps} — increments spanning more than 1.5 times the median
spacing (see \code{\link[=event_clock]{event_clock()}}).
\item \strong{Outliers} — largest absolute log-odds increment relative to the
robust scale of all increments; values far above the truncation
threshold deserve a manual look (data error vs. genuine news).
}
}
\section{Functions}{
\itemize{
\item \code{print(ec_validation)}: Print method; formatted report.

}}
\examples{
ec_validate(as_event_prices(brexit2016, time = "date", price = "q_leave"))
}
