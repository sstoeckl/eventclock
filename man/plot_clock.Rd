% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_clock}
\alias{plot_clock}
\title{Clock plot: the cumulative event clock}
\usage{
plot_clock(path, normalize = TRUE, ...)
}
\arguments{
\item{path}{An \code{event_clock_path} object from \code{\link[=event_clock_path]{event_clock_path()}} (an
\code{event_prices} object is converted automatically).}

\item{normalize}{Logical; plot the clock normalized to \eqn{[0,1]}
(default \code{TRUE}) or in raw units of \code{A}.}

\item{...}{Unused.}
}
\value{
A ggplot object.
}
\description{
Plots cumulative event-clock time \eqn{\widehat A_t} against calendar
time — the "clock plot": how much of the window's information had
arrived by each date.
}
\examples{
data(brexit2016)
ep <- as_event_prices(brexit2016, time = "date", price = "q_leave",
                      event_date = as.Date("2016-06-23"))
plot_clock(event_clock_path(ep))
}
