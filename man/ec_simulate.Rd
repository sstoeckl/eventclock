% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition.R
\name{ec_simulate}
\alias{ec_simulate}
\title{Simulate the exact transition to resolution}
\usage{
ec_simulate(n, q, A)
}
\arguments{
\item{n}{Integer, number of draws.}

\item{q}{Numeric vector of current event probabilities in \eqn{(0,1)}.}

\item{A}{Numeric vector of event-clock time (non-negative).}
}
\value{
A tibble with columns \code{J} (0/1 outcome), \code{zeta}, \code{L_T}, and
\code{q_T}.
}
\description{
Draws the outcome indicator \eqn{J \sim \mathrm{Bernoulli}(q)} and the
terminal log-odds from the exact transition law
\eqn{L_T = L + (1\{J=1\} - 1/2)A + \sqrt{A}\,\zeta}. The martingale
property \eqn{E[q_T] = q} holds by construction (Bayes consistency of
the \eqn{\pm A/2} drift).
}
\examples{
set.seed(1)
sim <- ec_simulate(1e4, q = 0.195, A = 0.166)
mean(sim$q_T) # close to 0.195
mean(sim$J)   # close to 0.195
}
